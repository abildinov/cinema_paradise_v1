# 🎬 Cinema Paradise

**Полнофункциональная система управления кинотеатром с веб и мобильным интерфейсом**

---

## 🚀 Быстрый запуск

### Активация виртуального окружения:
```bash
.\venv\Scripts\activate
```

### Установка зависимостей:
```bash
pip install -r requirements.txt
```

### Запуск API сервера:
```bash
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

### Запуск фронтенда (в отдельном терминале):
```bash
cd frontend
python -m http.server 3000
```

### Запуск мобильного приложения (в отдельном терминале):
```bash
cd mobile_app
python -m http.server 3002 --bind 192.168.0.10
```

---

## 📱 Доступ к системе

- **🌐 Веб-интерфейс**: http://192.168.0.10:3000
- **📱 Мобильное PWA**: http://192.168.0.10:3001
- **🔗 API документация**:http://192.168.0.10:8000/docs
- **🔄 Альтернативная документация**: http://192.168.0.10:8000/redoc

---

## 🎯 Возможности системы

### ✅ **Основные функции:**
- 🎭 **Управление фильмами** - добавление, редактирование, каталог
- 🏢 **Управление кинотеатрами и залами** - создание, конфигурация
- 📅 **Сеансы** - планирование, управление временем и ценами
- 🎫 **Билеты** - бронирование, оплата, отмена
- 👤 **Пользователи** - регистрация, профили, роли
- ⭐ **Отзывы** - рейтинги и комментарии к фильмам
- 📊 **Статистика** - отчеты по продажам и посещаемости

### 🎨 **Технологии:**
- **Backend**: FastAPI + SQLAlchemy + SQLite
- **Frontend**: Vanilla JavaScript SPA + Material Design
- **Mobile**: Progressive Web App (PWA) с Service Worker
- **Авторизация**: JWT токены + OAuth2 форма
- **Тестирование**: pytest + httpx

---

## 🗄️ База данных

### Таблицы:
- **users** - пользователи системы
- **movies** - фильмы
- **cinemas** - кинотеатры  
- **halls** - залы
- **sessions** - сеансы
- **tickets** - билеты
- **reviews** - отзывы

---

## �� API Эндпоинты

### ✅ **Работающие эндпоинты (реализованы в main.py):**

#### 🏠 **Основные**
- `GET /` - главная страница API
- `GET /health` - проверка здоровья системы

#### 🔐 **Авторизация (`/auth`)**
- `POST /auth/login` - вход в систему (OAuth2 форма)
- `POST /auth/register` - регистрация пользователя
- `GET /auth/me` - профиль текущего пользователя

#### 🎭 **Фильмы (`/movies`)**
- `GET /movies` - список фильмов

#### 📅 **Сеансы (`/sessions`)**
- `GET /sessions` - список сеансов
- `GET /sessions/{session_id}/tickets` - билеты конкретного сеанса

#### 🎫 **Билеты (`/tickets`)**
- `POST /tickets` - забронировать билет
- `GET /tickets/my` - получить мои билеты (авторизация)

#### 👑 **Админ панель (`/admin`)**
- `GET /admin/users` - список пользователей (только админы)
- `GET /admin/tickets` - управление билетами (только админы)

#### 🔧 **Утилиты**
- `GET /demo/populate` - заполнить демо данными

---

### 📝 **Примечание:**
- Роутеры в папке `app/routers/` созданы, но **НЕ подключены** к основному приложению
- Работают только эндпоинты, определённые напрямую в `app/main.py`
- Всего активных эндпоинтов: **13**

---

## 📁 Структура проекта

```
cinema_api/
├── 📁 app/                       # Основной код API
│   ├── 📁 routers/               # Роутеры для эндпоинтов
│   │   ├── auth.py               # Авторизация
│   │   ├── tickets.py            # Билеты  
│   │   ├── movies.py             # Фильмы
│   │   ├── sessions.py           # Сеансы
│   │   ├── cinemas.py            # Кинотеатры
│   │   ├── halls.py              # Залы
│   │   └── reviews.py            # Отзывы
│   ├── 📁 models/                # Модели базы данных
│   ├── 📁 schemas/               # Pydantic схемы
│   ├── database.py               # Подключение к БД
│   └── main.py                   # Главный файл API
├── 📁 frontend/                  # Веб-приложение
│   ├── 📁 js/                    # JavaScript модули
│   ├── 📁 css/                   # Стили
│   └── index.html                # Главная страница
├── 📁 mobile_app/                # PWA мобильное приложение
│   ├── index.html                # Мобильный интерфейс
│   ├── manifest.json             # PWA манифест
│   └── sw.js                     # Service Worker
├── 📁 tests/                     # Автотесты
│   └── test_endpoints.py         # Тесты эндпоинтов
├── 📁 data/                      # База данных
│   └── cinema.db                 # SQLite БД
├── requirements.txt              # Зависимости Python
└── README.md                     # Документация
```

---

## 🔧 Установка и настройка

### Требования:
- **Python 3.8+**
- **pip** (менеджер пакетов Python)

### Пошаговая установка:

1. **Клонирование проекта:**
   ```bash
   git clone <repository_url>
   cd cinema_api
   ```

2. **Создание виртуального окружения:**
   ```bash
   python -m venv venv
   .\venv\Scripts\activate  # Windows
   source venv/bin/activate  # Linux/Mac
   ```

3. **Установка зависимостей:**
   ```bash
   pip install -r requirements.txt
   ```

4. **Запуск API сервера:**
   ```bash
   uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
   ```

---

## 🎭 Демо данные и пользователи

### Тестовые аккаунты:
- **Администратор**: `admin` / `admin123`
- **Менеджер**: `manager` / `manager123`  
- **Пользователь**: `user` / `user123`

### Автосоздание данных:
При первом запуске автоматически создаются:
- Фильмы (Аватар, Титаник, Мстители и др.)
- Кинотеатры и залы
- Сеансы на ближайшие дни
- Тестовые билеты

---


### Тестирование через Swagger UI:
1. Откройте http://192.168.0.10:8000/docs
2. Протестируйте любой эндпоинт
3. Используйте кнопку "Authorize" для JWT токенов

---

## 🔐 Система авторизации

### Роли пользователей:
- **customer** - обычный пользователь (бронирование билетов)
- **manager** - менеджер (управление сеансами, фильмами)
- **admin** - администратор (полный доступ к системе)

### JWT токены:
- Авторизация через OAuth2 форму
- Токен действует 30 дней
- Единая авторизация между веб и мобильным приложением

---

## 📱 PWA мобильное приложение

### Установка:
1. Откройте http://192.168.0.10:3001 на мобильном
2. Нажмите "Добавить на главный экран"
3. Используйте как нативное приложение

### Возможности PWA:
- ✅ Работает офлайн
- ✅ Устанавливается на домашний экран
- ✅ Push-уведомления (готово к настройке)
- ✅ Быстрая загрузка с кэшированием

---

## 🚀 Продакшн деплой

### На VPS/сервере:
```bash
# Установка зависимостей
pip install -r requirements.txt

# Запуск с Gunicorn
pip install gunicorn
gunicorn app.main:app -w 4 -k uvicorn.workers.UvicornWorker --bind 0.0.0.0:8000

# Или с PM2
npm install -g pm2
pm2 start "uvicorn app.main:app --host 0.0.0.0 --port 8000" --name cinema-api
```

### С Docker:
```dockerfile
FROM python:3.9-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install -r requirements.txt
COPY . .
EXPOSE 8000
CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]
```

---

## 🛠️ Разработка

### Добавление нового эндпоинта:
1. Создайте роутер в `app/routers/`
2. Добавьте модель в `app/models/`
3. Создайте схемы в `app/schemas/`
4. Подключите роутер в `app/main.py`

### Структура роутера:
```python
from fastapi import APIRouter, Depends
from sqlalchemy.orm import Session
from app.database import get_db

router = APIRouter(prefix="/example", tags=["example"])

@router.get("/")
async def get_items(db: Session = Depends(get_db)):
    return {"message": "Hello World"}
```

---

## 📊 Мониторинг и логи

### Логирование:
Все SQL запросы и HTTP запросы логируются в консоль.

### Производительность:
- Используется connection pooling SQLAlchemy
- Кэширование статических файлов
- Lazy loading для связанных объектов

---

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для новой функции
3. Внесите изменения и добавьте тесты
4. Отправьте Pull Request

---

## 📄 Лицензия

MIT License - проект открыт для использования и модификации.

---

**🎬 Cinema Paradise** - современная система управления кинотеатром, готовая к продакшену! 🚀

**Версия**: 2.0  
**Последнее обновление**: Июнь 2025  
**Автор**: Команда разработки Cinema Paradise 
